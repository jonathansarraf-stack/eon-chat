services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: eon_chat
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - eon_chat_pgdata:/var/lib/postgresql/data

volumes:
  eon_chat_pgdata:
